import { SiteHeader } from '@/components/SiteHeader';
import { indicators } from '@/lib/public-evidence';
import { dimensions } from '@/lib/assessment-data';
export default function EvidenceDictionary(){return <><SiteHeader/><main className="shell evidence-dictionary"><span className="eyebrow">Methodology · version 1.0.0</span><h1>PAIR indicator dictionary</h1><p>Public sources establish context. Readiness requires use-specific evidence and human judgment. The eight automated indicators do not assign maturity scores.</p>{indicators.map(i=><article key={i.id}><h2>{i.title}</h2><p><strong>{i.automation} · {i.evidenceRole}</strong><br/>{i.dimensionIds.map(id=>dimensions.find(d=>d.id===id)?.title).join(' · ')}</p><p>{i.definition}</p><dl>{Object.entries({Source:i.source,Units:i.units,Geography:i.geography,'Update frequency':i.updateFrequency,Calculation:i.calculation,Limitations:i.limitations,'Human review':i.reviewRequirement}).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}</dl></article>)}</main></>;}
